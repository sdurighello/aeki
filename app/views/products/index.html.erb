<h1>Products</h1>

<% @products.each do |product| %>
  <p>
    <%= image_tag(product.image, alt: product.name)%><br />
    <%= link_to product.name, product_path(product.id) %>
  </p>
  <br />
<% end %>
